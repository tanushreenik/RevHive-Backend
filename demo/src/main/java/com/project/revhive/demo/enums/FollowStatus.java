package com.project.revhive.demo.enums;

public enum FollowStatus {
    PENDING,    // For private accounts (follow request)
    ACCEPTED,   // Following
    REJECTED,   // Request rejected
    BLOCKED     // Blocked user
}